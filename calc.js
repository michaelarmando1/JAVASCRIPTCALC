// Addition
function add(a, b) {
    return a + b;
}

console.log(add(5,4))
  
  // Subtraction
  function subtract(a, b) {
    return a - b;
  }
  
  // Multiplication
  function multiply(a, b) {
    return a * b;
  }
  
  // Division
  function divide(a, b) {
    if (b === 0) {
      return "Cannot divide by zero";
    }
    return a / b;
  }